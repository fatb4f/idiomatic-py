# Drills

1. Define a function `validate_ids(ids: list[str]) -> list[str]` that:
   - Rejects empty lists
   - Rejects duplicates
   - Returns the list unchanged if valid

2. Write tests covering:
   - Empty input
   - Duplicate IDs
   - Valid IDs
